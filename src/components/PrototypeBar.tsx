export function PrototypeBar() {
  return (
    <div className="border-b border-black/[0.06] bg-mist text-muted">
      <div className="page-shell flex min-h-9 items-center justify-center gap-4 py-2 text-[0.8rem] md:justify-between">
        <p className="text-center md:text-left">
          Independent prototype. Not an official government website.
        </p>
        <p className="hidden md:block">Fictional data only.</p>
      </div>
    </div>
  )
}
